#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int n, m, k;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    cin >> n >> m >> k;
    for(int i=0; i<m; i++) {
        int a, b, c; cin >> a >> b >> c;
    }
}